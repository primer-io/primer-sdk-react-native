--- a/ios/Sources/NativePrimer/RCTNativePrimer.mm
+++ b/ios/Sources/NativePrimer/RCTNativePrimer.mm
@@ -197,11 +197,7 @@ - (void)showPaymentMethod:(nonnull NSString *)paymentMethod intent:(nonnull NSSt
 #pragma mark - Event Emitting
 
 - (void)sendEventWithName:(NSString *)eventName body:(NSDictionary * _Nullable)body {
-#ifdef RCT_NEW_ARCH_ENABLED
-    [self emitOnEvent:@{@"eventType": eventName, @"data": body ?: @{}}];
-#else
     [super sendEventWithName:eventName body:body];
-#endif
 }
 
 - (NSArray<NSString *> *)supportedEvents {
--- a/src/specs/NativePrimer.ts
+++ b/src/specs/NativePrimer.ts
@@ -1,4 +1,4 @@
-import type { TurboModule, CodegenTypes } from 'react-native';
+import type { TurboModule } from 'react-native';
 import { TurboModuleRegistry } from 'react-native';
 
 export type EmittedEvent = {
@@ -40,7 +40,9 @@ export interface Spec extends TurboModule {
   addListener(eventName?: string): void;
   removeListeners(count?: number): void;
 
-  readonly onEvent: CodegenTypes.EventEmitter<EmittedEvent>;
+  onEvent(
+    callback: (event: EmittedEvent) => void
+  ): void;
 }
 
 // module name (NativePrimer) must be same in RCTNativePrimer.mm when exporting
